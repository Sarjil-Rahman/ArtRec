# Artifacts

This folder contains small bundled demo artifacts so the API can run immediately after clone/install.

The artifacts are generated from synthetic data and can be regenerated with:

```bash
python scripts/build_all.py
```
